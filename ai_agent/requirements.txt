# Working requirements.txt - Resolves dependency conflicts
# Simplified approach to avoid version conflicts

# Core LangChain - let pip resolve compatible versions
langchain>=0.1.0
langchain-core  # Let pip choose compatible version
langchain-community>=0.0.20

# New packages to fix deprecation warnings - minimal versions
langchain-chroma
langchain-huggingface  
langchain-ollama

# LLM providers
langchain-anthropic

# Keep Pydantic v1 for compatibility
pydantic>=2.0.0

# Web interface
streamlit>=1.28.0

# Vector database
chromadb>=0.4.0,<0.5.0

# Embedding models
sentence-transformers>=2.2.0
transformers>=4.30.0

# Web scraping and search
beautifulsoup4>=4.12.0
requests>=2.31.0
duckduckgo-search>=3.8.0

# Utilities
pyyaml>=6.0
plotly>=5.15.0
pandas>=2.0.0

# System dependencies
numpy>=1.24.0
tokenizers>=0.13